# TEAM — SSMS Data Analyzer (M0/M1)

Lead-owned status board. Agents do not edit this file.

Scope locked by the user: **SSMS 22 only**, **exact `COUNT(DISTINCT)` only**.

## Roster

| Agent | Role | Model | Why this model | Owns |
|---|---|---|---|---|
| **A** | core-engine | Opus | The crown jewels: dynamic multi-aggregate SQL generation, the exact-distinct planner, cancellation with partial-result retention. Most reasoning-dense work in the project. | `src/SsmsDataAnalyzer.Core/`, `src/SsmsDataAnalyzer.Cli/` |
| **C** | oe-spike | Opus | Undocumented-API archaeology across two multi-megabyte assemblies, plus a judgement call the whole M3 milestone hangs on. Needs to reach a defensible verdict, not a summary. | `spikes/OeProbe/`, `docs/oe-api.md` |
| **B** | vsix-shell | Sonnet | Well-specified scaffolding against a known manifest pattern, with two working example extensions on disk to copy from. Mechanical, not ambiguous. | `src/SsmsDataAnalyzer.Vsix/` |
| **D** | test-data | Sonnet | Schema design + T-SQL + xUnit. Laborious and detail-heavy, but the shape is fully specified up front. | `tools/seed/`, `tests/` |
| **Lead** | integration | — | Contract ownership, conflict routing, verification of claims, solution assembly. | `*.sln`, `PLAN.md`, `CONTRACT.md`, `TEAM.md`, `Directory.Build.props` |

## Why this partition

Directory ownership is **exclusive and disjoint** — this is not a git repo, so there are no
worktrees or merge machinery to fall back on. Two agents writing the same file would silently
clobber each other. Every agent was told: never touch a path outside your own, report cross-area
needs to the lead instead.

The shared model types were frozen in `CONTRACT.md` **before** any agent started, so four
parallel streams converge on one set of shapes. Agents B and D are deliberately coding against
types that Agent A is writing concurrently — they were told Core may not compile yet and to
proceed anyway. That is the whole point of freezing the contract first.

## Dependency map

```
CONTRACT.md (lead, frozen)
    ├─> A: Core + CLI ──────────┐
    ├─> B: VSIX shell ──────────┤ (B and D compile against A's types)
    ├─> D: seed DB + tests ─────┘
    └─> C: OE spike  (independent — gates M3 only, blocks nobody now)
```

Agent D's seeded database `SsmsDataAnalyzerTest` is the shared ground truth. D owns database
creation exclusively; no other agent may create or drop databases on the live instance.

## Known blockers going in

1. ~~**VS extension-development workload is not installed.**~~ **RESOLVED — but not for the
   reason first reported.** Agent B concluded the workload was unnecessary because
   `Microsoft.VSSDK.BuildTools` restored cleanly. Lead spike found that incomplete: the
   package's auto-imported targets file is **18 lines that only set environment variables** and
   carries no packaging logic. The real targets ship in the same package at
   `tools/vssdk/Microsoft.VsSDK.targets` and are never imported automatically — legacy projects
   reach them via `$(VSToolsPath)`, which resolves into the VS install and *does* need the
   workload. Confirmed no `Microsoft.VsSDK.targets` exists under either VS 2022 install here.
   With an explicit import of the NuGet copy, **`VSCommandTable.cto` is produced** — VSCT
   compilation works with no workload. Recorded as Amendment 5. Conclusion stands (no admin
   install needed), mechanism corrected. Pkgdef/.vsix creation still blocked on `VSSDK1207`,
   assigned to B.

2. ~~**Object Explorer context menu is unverified on SSMS 22.**~~ **RESOLVED — Tier A works.** Agent C's verdict decides whether
   M3 ships Tier A (real right-click) or falls back to Tier B (tool-window pickers). Tier B is
   already being built by Agent B regardless, so a negative verdict costs the project nothing.

## Lead verification checklist (on each agent's return)

Claims get checked, not accepted:

- [ ] A: run the CLI myself against the live instance — confirm real output, not a described run.
- [ ] A: grep the whole repo for `APPROX_COUNT_DISTINCT` — must be zero hits (contract rule 6).
- [ ] A: confirm sampling forces `DistinctCount = null` with the exact contracted `SkipReason`.
- [ ] A: confirm cancellation returns partial results rather than discarding completed batches.
- [ ] D: spot-check ground-truth numbers in `expected.md` against my own `sqlcmd` queries —
      especially the differing per-column last-fill dates, which are the headline feature.
- [ ] D: confirm the empty-table case doesn't divide by zero in percentage derivation.
- [ ] B: confirm the manifest pins `Microsoft.VisualStudio.Ssms` `[22.0,)` + amd64 and that no
      SSMS assembly is copied into VSIX output (`<Private>false</Private>`).
- [ ] C: confirm the verdict is backed by quoted evidence from the actual binaries/pkgdefs,
      not from recollection or an uncorroborated web result.
- [ ] Cross-cutting: no agent wrote outside its lane.

## Then

Lead assembles `SsmsDataAnalyzer.sln`, adds `Directory.Build.props` (held back until agents are
done so it cannot break their in-flight builds), runs the full test suite, and reports M1 status.

---

## Status — M1

| Agent | State | Evidence |
|---|---|---|
| **A** core-engine | ✅ Done — all 6 amendments landed | Lead re-ran ground-truth SQL and the CLI independently; figures match exactly |
| **D** test-data | ✅ Done — **61/61** green | Lead ran the suite independently: 61 passed, 0 failed |
| **B** vsix-shell | Scaffold complete; VSCT compile proven; packaging blocked | `.cto` produced in lead spike; `VSSDK1207` outstanding |
| **C** oe-spike | ✅ **Tier A VERDICT: FEASIBLE, no private reflection needed** | Lead re-verified with C's own probe: `HierarchyObject.AddChild` public, all 4 interfaces public, Red Gate SQL Prompt 11 shipping the same pattern in this install |

### Verified by lead, not merely reported

- `dbo.Orders`: 1,000 rows / 750 filled / **251** distinct on `ColStringBlank`.
  The 251 (not 252) is real — `''` and `'   '` collapse into one DISTINCT group. Agent D caught
  this only by running the query instead of trusting the derivation.
  **[Corrected later]** This was first attributed to collation. It is not: trailing-space
  insignificance is ANSI padding at the engine level and holds even under `_BIN2`. See the
  Amendment 11 correction. The number 251 is right; the original explanation was not.
- **Headline feature confirmed working:** per-column last-fill dates genuinely differ —
  `ColStoppedDay10` → 2024-01-10, `ColStoppedDay30` → 2024-01-30, `ColFilledAlways` → 2024-02-19.
- `dbo.WideTable` is 162 columns, exercising the 60-column pass-1 chunking for real.
- `dbo.EmptyTable` is genuinely 0 rows, which is what surfaced Amendment 2.
- Banned-token sweep: `APPROX_COUNT_DISTINCT` appears in exactly one file — D's test asserting
  its absence. Clean.

### Amendments issued this round

1. `ColumnProfile.TotalRowsContext` made public — an internal value made hand-built profiles
   report `Flags = None`, turning flag-derivation unit tests into false passes.
2. Flags suppressed entirely on empty tables — at 0 rows `Dead` degenerates into restating the
   row count rather than reporting a finding about a column.
3. Numeric literals permitted where T-SQL rejects parameters (`TABLESAMPLE`, `MAX_GRANT_PERCENT`,
   `MAXDOP` all fail with error 497 on variables) — approved under rule 2's existing carve-out.
4. `AggregateSupport.NoDistinct` implies `NoMinMax`; type matrix adopted from A's empirical
   testing against the live instance.
5. VSIX packaging requires the explicit `Microsoft.VsSDK.targets` import.

### Amendment 6 — the spike overturned a plan assumption

PLAN.md specified late-bound reflection for Tier A as insurance against an unsupported API that
might not exist in the VS 18 shell. Agent C proved the assumption wrong in the useful direction:
the API exists and **every type on the path is public**. Compile-time references replace
reflection.

The spike also caught a trap that would have cost days: **the OE node context menu is a WinForms
`ContextMenuStrip`**, not a `.vsct` menu. A `.vsct` group parented to the OE context menu
compiles fine, installs fine, and silently never appears. `.vsct` is now removed from the Tier A
design and kept only for Tier B/C.

---

## Final status — M1 complete

| Agent | Result |
|---|---|
| **A** core-engine | Core + CLI complete; Amendments 1,2,3,4,8,9,10,11,12 implemented and verified live |
| **B** vsix-shell | VSIX builds, packages, payload clean (9 entries, 139 KB); guard proven to fail on demand |
| **C** oe-spike | Tier A verdict: FEASIBLE, no private reflection — highest project risk eliminated |
| **D** test-data | Seeded DB + **66/66** green in 4 s; DB footprint 8 MB + 8 MB |

Everything above was re-verified by the lead independently, not accepted on report.

## What the parallel structure actually bought

Nearly every defect found was at a **seam** — no single agent could have seen it:

- The VSIX shipped `Microsoft.Data.SqlClient` **5.2.2** into a host already running **6.1.5**,
  with the native SNI library missing entirely. Found by unzipping B's artifact and comparing it
  against the SSMS install. The package built clean and would have failed on first click.
- Bumping Core to 6.1.5 (A) pulled a *different* transitive set, leaking eight more assemblies
  into B's payload. Had those two changes landed in the other order, the exclusion list would
  have been silently incomplete.
- D's test project pinned the SqlClient version independently of Core, so A's bump turned the
  suite red with `NU1605`.
- A pass-1 timeout discarded the whole profile while the distinct pass degraded gracefully —
  an inconsistency visible only by comparing the two passes side by side.

## Corrections that went the other way

The lead was wrong twice, and both were caught by agents doing the right thing:

1. **Amendment 5** — I recorded B's "the VS workload isn't needed" conclusion as fact. True in
   substance, wrong in mechanism, and the mechanism was the blocker: the auto-imported targets
   file is 18 lines of environment variables, and the real packaging targets need an explicit
   import.
2. **Amendment 11** — I asserted trailing-space distinct behaviour diverges under `_BIN2`. It
   does not; that is ANSI padding, not collation. **Agent D tested the hypothesis instead of
   seeding against it, and reported the discrepancy rather than fabricating data to match the
   contract.** Agent A reached the same correction independently; the lead reproduced it a third
   time.

The second is the one worth keeping: a plausible claim from the lead entered a binding contract
untested and would have produced a test asserting a divergence that does not exist.
**Test the hypothesis before it becomes a requirement.**

## The discipline that found the real bugs

Every significant catch came from the same habit — *run it, don't derive it*:

- D got **251** distinct, not the obvious 252, by executing the query.
- D proved the banned-token guard worked by narrowing it until it caught its own test files.
- B proved the payload guard worked by disabling suppression and watching the build fail on 42 files.
- A proved collation safety by first producing `Msg 468` **on demand** from a negative control —
  because "no error" means nothing unless the test could have produced one.

## Still not proven

**The extension has never run inside SSMS.** Everything verified is build-time or CLI-level:
the engine is thoroughly proven, the VSIX is proven only to build and package correctly.
Loading it into SSMS 22 is the next milestone and the next place defects will surface.
